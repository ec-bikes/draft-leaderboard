import type { Group } from './types/Group.js';

export const baseUrl = '/draft-leaderboard/';

/** Available draft years starting with most recent */
export const years = [2025, 2024];

export const groups: Group[] = ['women', 'men'];
